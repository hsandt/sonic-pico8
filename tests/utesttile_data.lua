require("bustedhelper")
require("game/data/tile_data")

describe('tiledata', function ()

  describe('setup_map_data', function ()
    pending('should call setup on tile_test_data (busted only)', function ()
    end)
  end)

  describe('teardown_map_data', function ()
    pending('should call teardown on tile_test_data (busted only)', function ()
    end)
  end)

  describe('clear_map', function ()
    pending('should call clear_map from pico8api (busted only)', function ()
    end)
  end)

end)
