-- table mapping tile sprite id locations to tile collision masks
-- this will be completed as tiles are added, adding extra information
--  such as mirror-y: bool for upside-down tiles
sprite_to_collision_masks = {
  [sprite_id_location(0, 4)] = sprite_id_location(0, 5)
}
